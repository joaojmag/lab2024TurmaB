algoritmo "FazSomaProce"
var
   x,y:inteiro
procedimento soma(a,b:inteiro)
inicio
      escreval("Recebi o valor ",a)
      escreval("Recebi o valor ",b)
      escreval("A soma dos valores é:",a+b)

fimprocedimento
inicio
      x<-5
      y<-3
      soma(x,y)
fimalgoritmo