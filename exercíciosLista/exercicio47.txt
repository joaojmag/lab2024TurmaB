Algoritmo "soma500ao0"

Var
   cont, soma: inteiro
Inicio
   soma <- 0
   cont <- 500
   Enquanto (cont >= 0) faca
      Se(cont > 0)entao
         Escreva(cont, "+")
         Soma <- soma + cont
      Senao
         Escreva(cont)
         Soma <- soma + cont
      FimSe
      cont <- cont - 50
   FimEnquanto
   Escreva("=", soma)
Fimalgoritmo
