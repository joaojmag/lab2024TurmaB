Algoritmo "analiseGrupo7"

Var
   cont, maisNoventa, menosCinquenta, maisCem: inteiro
   media, peso, altura, soma: real
Inicio
   cont <- 1
   soma <- 0
   menosCinquenta <- 0
   maisNoventa <- 0
   maisCem <- 0
   Enquanto (cont <= 7) faca
      Escreval("Qual seu peso (em kg)")
      Leia(peso)
      Escreval("Qual sua altura (em metros)?")
      Leia(altura)
      soma <- soma + altura
      Se(peso > 90)entao
         maisNoventa <- maisNoventa + 1
      FimSe
      Se(peso < 50) e (altura < 1.60) entao
         menosCinquenta <- menosCinquenta + 1
      FimSe
      Se(peso > 100) e (altura > 1.90)entao
         maisCem <- maisCem + 1
      FimSe
      cont <- cont + 1
   FimEnquanto
   media <- soma / 7
   Escreval("Média de altura do grupo: ", media)
   Escreval("Pessoas +90 kg: ", maisNoventa)
   Escreval("Pessoas -50kg e -1.60m: ", menosCinquenta)
   Escreval("Pessoas +1.90m e +100kg: ", maisCem)
Fimalgoritmo
