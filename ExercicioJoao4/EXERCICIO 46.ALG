algoritmo "exercicio 46"
var
cont,soma:inteiro
inicio
      soma<-6
      cont<-6
      enquanto (cont<=98) faca
          cont<-cont+2
          escreval(soma, " + ", cont, " = ", cont+soma)
          soma<-cont+soma
      fimenquanto

fimalgoritmo