algoritmo "semnome"
var
m: caractere

procedimento gerador(b: caractere)
inicio
escreval("-------------------------")
escreval(b)
escreval("-------------------------")
FimProcedimento
inicio
escreval("Digite a mensagem que você quer que apareça na tela")
leia(m)
limpatela
escreval(m)
fimalgoritmo