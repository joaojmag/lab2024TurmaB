algoritmo "semnome"
var
contador,soma: inteiro
inicio
contador <- 500
soma <- 0
Enquanto (contador >= 0) faca
escreva(contador)
soma <- soma + contador
contador <- contador - 50
FimEnquanto
escreval(soma)
FimAlgoritmo