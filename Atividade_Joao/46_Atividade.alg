algoritmo "semnome"
var
contador,soma: inteiro
inicio
contador <- 6
soma <- 0
Enquanto (contador <= 100)faca
escreva(contador)
soma <- soma + contador
contador <- contador + 2
FimEnquanto
escreval(soma)
FimAlgoritmo