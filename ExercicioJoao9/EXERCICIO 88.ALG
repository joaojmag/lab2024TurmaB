algoritmo "Exercicio 88"

var

cont:inteiro
b:inteiro
procedimento gerador(a:caractere )
inicio
      escreval("+-------=======------+")
      para b<-1 ate cont faca
      escreval(a)
      fimpara
      escreval("+-------=======------+")
fimprocedimento

inicio
      escreval("Quer ver ate quantas vezes?")
      leia(cont)
      gerador("Aprendendo portugol")

fimalgoritmo
